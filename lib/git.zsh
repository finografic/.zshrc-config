# ============================================================================ #
# GIT UTILITIES INDEX - Loads all git modules from lib/git/
# ============================================================================ #

# Source core git operations
source "$ZSHRC_ROOT/lib/git/git.core.zsh"

# Source commit operations
source "$ZSHRC_ROOT/lib/git/git.commit.zsh"

# Source rebase operations
source "$ZSHRC_ROOT/lib/git/git.rebase.zsh"

# Source maintenance operations
source "$ZSHRC_ROOT/lib/git/git.maintenance.zsh"

# Source submodule operations
source "$ZSHRC_ROOT/lib/git/git.submodule.zsh"

# Source stash operations
source "$ZSHRC_ROOT/lib/git/git.stashes.zsh"

# Source tags operations
source "$ZSHRC_ROOT/lib/git/git.tags.zsh"
