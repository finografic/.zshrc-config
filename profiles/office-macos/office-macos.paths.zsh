# TODO: populate per employer. Not sourced by default — add a
# `source "$ZENV_PATH/$ZENV.paths.zsh"` line to office-macos.zsh once this
# has real content.

# Example:
# export PATH="$HOME/bin/work-tools:$PATH"
