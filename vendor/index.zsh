#!/bin/zsh
# ============================================================================ #
# NOTE: VENDOR INDEX - Package managers and runtime tools
# ============================================================================ #

# pnpm PATH / PNPM_HOME (helpers: lib/node/pnpm.zsh)
source "$ZSHRC_ROOT/vendor/pnpm-path.zsh"

# nvm (Node Version Manager)
source "$ZSHRC_ROOT/vendor/nvm.zsh"

