# ========================================================================= #
# GIT UTILITIES INDEX
# ========================================================================= #

# Source core git operations
source "$ZSHRC_ROOT/lib/git.core.zsh"

# Source commit operations
source "$ZSHRC_ROOT/lib/git.commit.zsh"

# Source rebase operations
source "$ZSHRC_ROOT/lib/git.rebase.zsh"

# Source maintenance operations
source "$ZSHRC_ROOT/lib/git.maintenance.zsh"

# Source submodule operations
source "$ZSHRC_ROOT/lib/git.submodule.zsh"

# Source stash operations
source "$ZSHRC_ROOT/lib/git.stashes.zsh"

# Source tags operations
source "$ZSHRC_ROOT/lib/git.tags.zsh"
