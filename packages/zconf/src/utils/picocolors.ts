import picocolors from 'picocolors';

/**
 * Shared styling alias. Import `pc` from here rather than importing
 * `picocolors` directly, so call sites stay consistent and greppable.
 */
export const pc = picocolors;
