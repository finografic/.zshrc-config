# Security

This is a personal dotfiles configuration, shared publicly for reference. It is not a
production service and carries no SLA.

If you find a security issue (a credential leak, an unsafe default, a shell-injection
vector in a script) please open an issue describing it. There is no bug bounty; this repo
is maintained on a best-effort basis.
